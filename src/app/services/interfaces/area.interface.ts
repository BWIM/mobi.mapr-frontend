export interface Area {
    id: number;
    name: string;
    display_name?: string;
    // Weitere Eigenschaften können später hinzugefügt werden, wenn sie für die grafische Darstellung benötigt werden
} 